media_library_primary_tag = album_artist

progressbar_look = =>?

#user_interface = alternative

mouse_support = no
header_visibility = no
#display_bitrate = no
#display_volume_level = no
empty_tag_marker = <none>

ignore_leading_the = yes
jump_to_now_playing_song_at_start = yes

song_columns_list_format = (50)[green]{t:title} (20)[blue]{a:artist} (20)[magenta]{b:album} (6f)[white]{lr:time}

song_library_format = {%t}|{%f}

# alternative_header_first_line_format =
# alternative_header_second_line_format = {{$7$b%a$/b$9}{.$5%b$9}}|{%D}

now_playing_prefix = $b
now_playing_suffix = $/b

clock_display_seconds = yes

screen_switcher_mode = playlist, media_library

main_window_color = green
